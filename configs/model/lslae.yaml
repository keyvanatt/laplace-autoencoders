name: lslae

# Paramètres pour le DataModule (construction de la transformée de Laplace)
N: 128
Nt: 150
K: 16            # fréquences Laplace (doit correspondre au LLAE source)
gamma_init: 0.01 # amortissement initial du contour de Bromwich

# latent_dim, dt, time_L → lus depuis le checkpoint AE (ckpt_utils.load_llae_from_ckpt)
# Architecture du surrogate (hidden_dim, head_dim, …) → dans training/surrogate_lslae.yaml
